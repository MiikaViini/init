# gets you real users homefolders
# does not work properly if have deleted user without -r
ls /home

# real users UID is between 1000 and 60000
# getent get entries from textfiles/databases
getent passwd {1000..60000}

